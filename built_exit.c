#include "tamagoshell.h"
#include <stdlib.h>

/**
 * built_exit - a function for exit the shell
 *Description: cause normal process termination
 *Return: return nothing
 */
void built_exit(void)
{
	exit(0);
}
